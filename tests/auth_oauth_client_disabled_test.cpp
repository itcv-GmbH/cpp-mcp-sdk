#include <catch2/catch_test_macros.hpp>
#include <mcp/auth/oauth_client.hpp>

TEST_CASE("OAuth client disabled state handling", "[auth][oauth][disabled]")
{
  // Placeholder test - will be expanded in future tasks
  // OAuth client is always available - this tests error handling when features are not configured
  REQUIRE(true);
}
