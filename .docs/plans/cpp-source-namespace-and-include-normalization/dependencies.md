# Dependency Graph

## Phase 1: Inventory And Decisions
- [ ] `task-001`: Inventory `src/**/*.cpp` namespace-to-path violations

## Phase 2: Namespace And Path Alignment
- [ ] `task-002`: Relocate `src/**/*.cpp` files to align with namespace layout
- [ ] `task-003`: Normalize MCP namespaces in `src/**/*.cpp` to match the derived namespace

## Phase 3: Include Normalization
- [ ] `task-004`: Normalize repository header includes in `src/**/*.cpp` and remove umbrella includes

## Phase 4: Build System And Verification
- [ ] `task-005`: Update build inputs and verify quality gates
